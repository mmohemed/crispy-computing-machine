'use strict';

module.exports = {
  slug: '02-variables',
  title: 'المتغيّرات وأنواع البيانات',
  summary: 'الأعداد والنصوص والرموز والقيم المنطقية و nil، وأنواع المتغيّرات الخمسة ونطاق كلٍّ منها.',
  duration: 50,
  level: 'مبتدئ',
  tags: ['متغيّرات', 'أنواع', 'نصوص'],
  objectives: [
    'تعرّف متغيّراً وتفهم أن Ruby لا يحتاج تصريحاً بالنوع.',
    'تميّز بين أنواع المتغيّرات الخمسة ونطاق كلٍّ منها.',
    'تتعامل مع الأعداد الصحيحة والعشرية والكسور بدقّة.',
    'تتقن النصوص: الاستيفاء والدوال الشائعة والقابلية للتعديل.',
    'تفهم متى تستخدم **الرمز** (Symbol) بدل النصّ.',
    'تفرّق بين `nil` والفراغ، وتتعامل معه بأمان.'
  ],
  quickRef: [
    { code: 'x = 5', desc: 'متغيّر محلي' },
    { code: '@x', desc: 'متغيّر مثيل' },
    { code: 'X = 5', desc: 'ثابت' },
    { code: '"a#{b}"', desc: 'استيفاء' },
    { code: ':name', desc: 'رمز' },
    { code: 'x.to_i / .to_s / .to_f', desc: 'تحويل' },
    { code: 'x.nil?', desc: 'اختبار nil' },
    { code: 'x&.length', desc: 'استدعاء آمن' }
  ],
  blocks: [
    { t: 'h2', text: 'المتغيّر ليس صندوقاً — إنه ملصق' },
    { t: 'p', text: 'في Ruby لا تصرّح بنوع المتغيّر ولا تحجز له مساحة. المتغيّر مجرّد **اسم يشير إلى كائن** موجود في الذاكرة. هذا الفهم يوفّر عليك أخطاءً كثيرة لاحقاً.' },
    { t: 'code', lang: 'ruby', code: `
x = 5           # x يشير إلى الكائن 5
x = "نصّ"       # الآن يشير إلى نصّ — لا مشكلة
x = [1, 2, 3]   # والآن إلى مصفوفة

a = "مرحبا"
b = a           # b و a يشيران لنفس الكائن!
b << " بك"      # نعدّل الكائن نفسه
puts a          # => "مرحبا بك"  ← تفاجأت؟

a.object_id == b.object_id   # => true
`.trim() },
    { t: 'warn', title: 'الفخّ الأشهر للمبتدئين', text: 'الإسناد `b = a` **لا ينسخ** الكائن، بل ينسخ المرجع. إن أردت نسخة مستقلة استخدم `b = a.dup`.' },
    { t: 'code', lang: 'ruby', code: `
a = "مرحبا"
b = a.dup       # نسخة مستقلة
b << " بك"
puts a          # => "مرحبا"   ← لم يتأثّر
`.trim() },

    { t: 'h2', text: 'أنواع المتغيّرات الخمسة' },
    { t: 'p', text: 'Ruby يميّز نوع المتغيّر من **أول حرف في اسمه** لا من كلمة مفتاحية. هذا اختصار أنيق يخبرك بنطاق المتغيّر بمجرّد رؤيته.' },
    { t: 'table', head: ['النوع', 'الصياغة', 'النطاق', 'الاستخدام'], rows: [
      ['**محلي**', '`name`', 'الكتلة أو الدالة الحالية فقط', 'الأغلبية الساحقة'],
      ['**مثيل**', '`@name`', 'الكائن الواحد', 'حالة الكائن — سنستخدمه كثيراً في OOP'],
      ['**صنف**', '`@@count`', 'الصنف وكل مثيلاته وورّاثه', 'نادر؛ يُفضَّل تجنّبه'],
      ['**عام**', '`$debug`', 'البرنامج كلّه', 'تجنّبه إلا لأغراض خاصة'],
      ['**ثابت**', '`MAX`', 'الصنف/الوحدة، ومقروء من الخارج', 'قيم لا تتغيّر']
    ]},
    { t: 'code', lang: 'ruby', code: `
class Counter
  MAX = 100          # ثابت
  @@total = 0        # متغيّر صنف — مشترك بين كل المثيلات

  def initialize(name)
    @name  = name    # متغيّر مثيل — خاص بهذا الكائن
    @count = 0
    @@total += 1
  end

  def increment
    step = 1         # متغيّر محلي — يختفي بانتهاء الدالة
    @count += step
    raise "تجاوز الحدّ" if @count > MAX
    @count
  end

  def self.total = @@total
end

a = Counter.new("أول")
b = Counter.new("ثانٍ")
Counter.total        # => 2
Counter::MAX         # => 100
`.trim() },
    { t: 'tip', text: 'قاعدة عملية: استخدم المتغيّرات المحلية افتراضياً، ومتغيّرات المثيل لحالة الكائن، والثوابت للقيم الثابتة. أما متغيّرات الصنف `@@` والعامة `$` فتجنّبها — لهما بدائل أفضل دائماً.' },

    { t: 'h2', text: 'الأعداد' },
    { t: 'code', lang: 'ruby', code: `
42            # Integer — بلا حدّ أعلى!
3.14          # Float
1_000_000     # الشرطة السفلية للقراءة فقط
0xff          # => 255  (ستّ عشري)
0b1010        # => 10   (ثنائي)

2 ** 100      # => 1267650600228229401496703205376  ← بلا فيضان
`.trim() },
    { t: 'h3', text: 'فخّ القسمة الصحيحة' },
    { t: 'compare', lang: 'ruby', bad: {
      code: `total = 7
count = 2
average = total / count    # => 3   ✗ ضاع الكسر!`,
      why: 'قسمة عدد صحيح على عدد صحيح تُرجع عدداً صحيحاً في Ruby — يُقتطع الكسر بصمت.'
    }, good: {
      code: `total = 7
count = 2
average = total.fdiv(count)   # => 3.5
# أو
average = total / count.to_f  # => 3.5`,
      why: '`fdiv` تقسم قسمة عشرية دائماً، وهي أوضح من تحويل أحد المعاملين يدوياً.'
    }},
    { t: 'h3', text: 'دقّة العشرية' },
    { t: 'code', lang: 'ruby', code: `
0.1 + 0.2               # => 0.30000000000000004  ← ليست مشكلة Ruby
0.1 + 0.2 == 0.3        # => false !

# للمال والحسابات الدقيقة استخدم Rational أو BigDecimal
require 'bigdecimal'
require 'bigdecimal/util'

(0.1.to_d + 0.2.to_d) == 0.3.to_d   # => true

# أو الكسور النسبية — دقيقة تماماً
(1r/3) * 3              # => (1/1)
`.trim() },
    { t: 'danger', title: 'لا تخزّن المال في Float أبداً', text: 'خطأ التقريب يتراكم مع آلاف العمليات فينتج فرق حقيقي في الحسابات. خزّن المبالغ كـ **أعداد صحيحة بالهللات/السنتات** أو استخدم `BigDecimal`.' },
    { t: 'h3', text: 'دوال عددية مفيدة' },
    { t: 'code', lang: 'ruby', code: `
-7.abs        # => 7
7.5.round     # => 8
7.5.floor     # => 7
7.4.ceil      # => 8
3.14159.round(2)  # => 3.14
10.between?(1, 20)  # => true
7.even?       # => false
7.odd?        # => true
0.zero?       # => true
17.clamp(1, 10)     # => 10   ← يحصر القيمة في مدى
10.gcd(4)     # => 2   القاسم المشترك الأكبر
255.to_s(2)   # => "11111111"
"ff".to_i(16) # => 255
`.trim() },

    { t: 'h2', text: 'النصوص' },
    { t: 'p', text: 'النصّ في Ruby كائن غنيّ جداً بالدوال، وهو **قابل للتعديل** (Mutable) خلافاً لكثير من اللغات.' },
    { t: 'h3', text: 'التنصيص المزدوج مقابل المفرد' },
    { t: 'code', lang: 'ruby', code: `
name = "سارة"

"مرحباً #{name}"     # => "مرحباً سارة"   ← استيفاء يعمل
'مرحباً #{name}'     # => "مرحباً \#{name}"  ← نصّ حرفي

"سطر\\nجديد"          # => يطبع سطرين
'سطر\\nجديد'          # => يطبع \\n حرفياً
`.trim() },
    { t: 'tip', text: 'استخدم التنصيص المزدوج افتراضياً — الفرق في الأداء معدوم عملياً، والانتقال للاستيفاء لاحقاً لا يحتاج تعديل النوع.' },
    { t: 'h3', text: 'النصوص متعدّدة الأسطر' },
    { t: 'code', lang: 'ruby', code: `
# heredoc — <<~ يزيل المسافة البادئة المشتركة
message = <<~TEXT
  عزيزي العميل،

  تمّ استلام طلبك رقم #{123}.
  شكراً لثقتك.
TEXT

# <<~ (مع ~) هو ما تريده دائماً
# <<- يحافظ على المسافات البادئة
`.trim() },
    { t: 'h3', text: 'أهم دوال النصّ' },
    { t: 'code', lang: 'ruby', code: `
s = "  Ruby رائعة  "

s.strip           # => "Ruby رائعة"    يزيل الفراغات الطرفية
s.length          # => 15
s.upcase          # => "  RUBY رائعة  "
s.downcase        # => "  ruby رائعة  "
s.reverse
s.include?("Ruby")   # => true
s.start_with?("  ")  # => true
s.sub("Ruby", "روبي")    # يستبدل أول تطابق
s.gsub("a", "A")         # يستبدل كل التطابقات
s.split(" ")             # => ["Ruby", "رائعة"]
s.strip.chars            # => ["R","u","b","y"," ",...]
"abc" * 3                # => "abcabcabc"
"7".to_i                 # => 7
7.to_s                   # => "7"
:sym.to_s                # => "sym"
"%.2f" % 3.14159         # => "3.14"
format("مرحباً %s، عمرك %d", "سارة", 30)
`.trim() },
    { t: 'h3', text: 'دوال بعلامة `!`' },
    { t: 'code', lang: 'ruby', code: `
s = "ruby"

s.upcase      # => "RUBY"   لكن s نفسها لم تتغيّر
s             # => "ruby"

s.upcase!     # يعدّل s نفسها
s             # => "RUBY"

# انتبه: دوال ! تُرجع nil إن لم يحدث تغيير
"RUBY".upcase!   # => nil !
`.trim() },
    { t: 'danger', title: 'خطأ شائع', text: 'كتابة `name = name.strip!` قد تجعل `name` تساوي `nil` إن لم يكن هناك فراغ يُزال. لا تسند نتيجة دالة `!` لمتغيّر — استخدم النسخة بلا علامة تعجّب.' },
    { t: 'h3', text: 'الفهرسة والتقطيع' },
    { t: 'code', lang: 'ruby', code: `
s = "برمجة"

s[0]        # => "ب"
s[-1]       # => "ة"     من النهاية
s[0, 3]     # => "برم"   ابدأ من 0 وخذ 3
s[1..3]     # => "رمج"   مدى
s[1...3]    # => "رم"    مدى حصري
s[/م\\S+/]   # => "مجة"   بتعبير نمطي
`.trim() },

    { t: 'h2', text: 'الرموز (Symbols)' },
    { t: 'p', text: 'الرمز نصّ **خفيف وغير قابل للتعديل** يُنشَأ مرة واحدة في الذاكرة مهما تكرّر. يُكتب بنقطتين قبله: `:name`.' },
    { t: 'code', lang: 'ruby', code: `
"name".object_id == "name".object_id   # => false — كائنان مختلفان
:name.object_id  == :name.object_id    # => true  — نفس الكائن دائماً
`.trim() },
    { t: 'table', head: ['استخدمه رمزاً حين…', 'استخدمه نصّاً حين…'], rows: [
      ['يكون **معرّفاً** ثابتاً في الشيفرة', 'يكون **بيانات** من المستخدم أو الشبكة'],
      ['مفتاح Hash: `{ name: "سارة" }`', 'قيمة يعرضها المستخدم'],
      ['اسم دالة: `send(:save)`', 'نصّ ستعدّله أو تبني عليه'],
      ['حالة أو خيار: `status: :active`', 'محتوى يُطبع أو يُخزَّن']
    ]},
    { t: 'code', lang: 'ruby', code: `
# رموز كمفاتيح — الاصطلاح الشائع
user = { name: "سارة", role: :admin, active: true }
user[:name]     # => "سارة"

# النصّ لأن هذه بيانات
comment = "شكراً على المقال!"

# تحويل بين الاثنين
:admin.to_s     # => "admin"
"admin".to_sym  # => :admin
`.trim() },

    { t: 'h2', text: 'القيم المنطقية و nil' },
    { t: 'p', text: 'قاعدة Ruby في الصدق **بسيطة للغاية** وتختلف عن أغلب اللغات: **كل شيء صادق إلا `false` و `nil`**. لا استثناءات.' },
    { t: 'code', lang: 'ruby', code: `
if 0        then puts "0 صادق!"      end   # ✅ يُطبع
if ""       then puts "الفراغ صادق!" end   # ✅ يُطبع
if []       then puts "[] صادقة!"    end   # ✅ يُطبع
if nil      then puts "لن يُطبع"     end   # ❌
if false    then puts "لن يُطبع"     end   # ❌
`.trim() },
    { t: 'note', text: 'في JavaScript و Python يُعتبر `0` و `""` كاذبين. في Ruby هما صادقان. إن جئت من لغة أخرى فهذه أول عادة عليك تغييرها.' },
    { t: 'h3', text: 'التعامل الآمن مع nil' },
    { t: 'code', lang: 'ruby', code: `
user = nil

user.name              # ✗ NoMethodError
user&.name             # => nil  ← الاستدعاء الآمن
user&.name&.upcase     # => nil  ← يمكن تسلسله

# قيمة افتراضية
name = user&.name || "زائر"

# اختبارات
nil.nil?               # => true
"".empty?              # => true
nil.to_s               # => ""
nil.to_a               # => []
nil.to_i               # => 0

# الفرق بين الفارغ والفراغ
"".nil?                # => false !
"   ".strip.empty?     # => true
`.trim() },
    { t: 'compare', lang: 'ruby', bad: {
      code: `if user != nil && user.profile != nil && user.profile.bio != nil
  puts user.profile.bio
end`,
      why: 'ثلاثة اختبارات متداخلة — طويل ويصعب قراءته، ويتضاعف مع كل مستوى.'
    }, good: {
      code: `bio = user&.profile&.bio
puts bio if bio

# أو بقيمة افتراضية
puts user&.profile&.bio || "لا توجد نبذة"`,
      why: 'الاستدعاء الآمن `&.` يتوقّف عند أول `nil` ويُرجع `nil` مباشرة.'
    }},

    { t: 'h2', text: 'التحويل بين الأنواع' },
    { t: 'code', lang: 'ruby', code: `
"42".to_i        # => 42
"42abc".to_i     # => 42     ← يتساهل ويأخذ ما يستطيع
"abc".to_i       # => 0      ← خطر! لا يرمي خطأ

Integer("42")    # => 42
Integer("abc")   # ✗ ArgumentError  ← أفضل للمدخلات غير الموثوقة

"3.9".to_f       # => 3.9
3.9.to_i         # => 3      ← يقتطع لا يقرّب
3.9.round        # => 4

[1,2].to_a
{a: 1}.to_a      # => [[:a, 1]]
[[:a, 1]].to_h   # => {a: 1}
`.trim() },
    { t: 'tip', text: 'استخدم `to_i` حين تثق بالمدخل، و`Integer()` حين تريد أن ينفجر الخطأ فوراً بدل أن يتسلّل صفر خاطئ في بياناتك.' },

    { t: 'h2', text: 'التجميد (freeze)' },
    { t: 'code', lang: 'ruby', code: `
CONFIG = { host: "localhost", port: 3000 }.freeze
CONFIG[:port] = 4000    # ✗ FrozenError

NAME = "CodeWay".freeze
NAME << "!"             # ✗ FrozenError

CONFIG.frozen?          # => true
:sym.frozen?            # => true  الرموز مجمّدة أصلاً
42.frozen?              # => true  والأعداد كذلك
`.trim() },
    { t: 'note', text: 'أضف `# frozen_string_literal: true` كأول سطر في ملفاتك: يجمّد كل النصوص الحرفية تلقائياً فيقلّ استهلاك الذاكرة وتُكتشَف التعديلات غير المقصودة مبكّراً.' },

    { t: 'h2', text: 'تمرين' },
    { t: 'exercise',
      title: 'محلّل نصّ ومحوّل وحدات',
      brief: 'اكتب سكربتاً يحلّل نصّاً يدخله المستخدم ويعرض إحصاءات عنه، ثم يحوّل بين وحدات.',
      requirements: [
        'اطلب من المستخدم جملة، واعرض: عدد الحروف (مع الفراغات وبدونها)، عدد الكلمات، عدد الجمل.',
        'اعرض أطول كلمة وأقصر كلمة.',
        'اعرض الجملة معكوسة، وبأحرف كبيرة، وبصيغة `snake_case`.',
        'اطلب مبلغاً بالريال وحوّله للدولار بسعر 3.75، مع تقريب لخانتين.',
        'اطلب درجة حرارة مئوية وحوّلها لفهرنهايت.',
        'خزّن أسعار التحويل في **ثوابت مجمّدة** لا متغيّرات.',
        'تعامل بأمان مع المدخل الفارغ — لا تنهر البرنامج.',
        'استخدم `Integer()` أو `Float()` للتحقّق من المدخلات الرقمية.'
      ],
      hints: [
        '`text.split` تقسّم على أي فراغ وتتجاهل الفراغات المتعدّدة.',
        '`words.max_by(&:length)` تُرجع أطول كلمة.',
        '`text.count(" ")` تعدّ الفراغات، و `text.delete(" ").length` تعدّ الحروف بدونها.',
        '`rescue ArgumentError` يمسك خطأ التحويل الفاشل.'
      ],
      solution: { lang: 'ruby', code: `
# frozen_string_literal: true
# analyzer.rb — محلّل نصّ ومحوّل وحدات

SAR_TO_USD = 3.75
SEPARATOR  = ("─" * 46).freeze

def ask_number(prompt)
  print prompt
  Float(gets.chomp)
rescue ArgumentError
  puts "  ⚠️  مدخل غير رقمي — سنستخدم 0"
  0.0
end

# ===== 1) تحليل النصّ =====
print "اكتب جملة: "
text = gets.chomp

if text.strip.empty?
  puts "لم تكتب شيئاً!"
  exit
end

words     = text.split
sentences = text.split(/[.!?؟]/).reject { |s| s.strip.empty? }

puts
puts SEPARATOR
puts "تحليل النصّ".center(44)
puts SEPARATOR
puts "الحروف (مع الفراغ):  #{text.length}"
puts "الحروف (بلا فراغ):   #{text.delete(' ').length}"
puts "الكلمات:             #{words.size}"
puts "الجمل:               #{sentences.size}"
puts "أطول كلمة:           #{words.max_by(&:length)}"
puts "أقصر كلمة:           #{words.min_by(&:length)}"
puts SEPARATOR
puts "معكوسة:   #{text.reverse}"
puts "كبيرة:     #{text.upcase}"
puts "snake_case: #{text.strip.downcase.gsub(/\\s+/, '_')}"
puts SEPARATOR

# ===== 2) تحويل العملة =====
puts
amount = ask_number("المبلغ بالريال: ")
usd    = (amount / SAR_TO_USD).round(2)
puts "  #{amount} ريال = #{usd} دولار  (بسعر #{SAR_TO_USD})"

# ===== 3) تحويل الحرارة =====
puts
celsius    = ask_number("الحرارة بالمئوية: ")
fahrenheit = (celsius * 9.0 / 5 + 32).round(1)
puts "  #{celsius}°C = #{fahrenheit}°F"
`.trim() }
    },

    { t: 'h2', text: 'اختبر نفسك' },
    { t: 'quiz', items: [
      { q: 'ماذا يحدث بعد `b = a` ثم `b << "x"` حين تكون `a` نصّاً؟', options: ['a لا تتأثّر', 'a تتغيّر أيضاً لأن المتغيّرين يشيران لنفس الكائن', 'خطأ', 'يُنشَأ كائن جديد'], answer: 1,
        explain: 'الإسناد ينسخ المرجع لا الكائن؛ استخدم `dup` لنسخة مستقلة.' },
      { q: 'ما نتيجة `7 / 2` في Ruby؟', options: ['3.5', '3 — لأن قسمة الصحيح على الصحيح تُرجع صحيحاً', '4', 'خطأ'], answer: 1,
        explain: 'استخدم `7.fdiv(2)` أو `7 / 2.0` للحصول على 3.5.' },
      { q: 'أيّ القيم كاذبة في Ruby؟', options: ['0 و "" و nil', '`false` و `nil` فقط', '0 و nil', '[] و {} و nil'], answer: 1,
        explain: 'قاعدة Ruby أبسط من أغلب اللغات: كل شيء صادق إلا `false` و `nil`.' },
      { q: 'متى تستخدم الرمز `:name` بدل النصّ `"name"`؟', options: ['دائماً', 'حين يكون معرّفاً ثابتاً في الشيفرة كمفاتيح Hash أو أسماء الحالات', 'حين يكون النصّ قصيراً', 'مع الأعداد'], answer: 1,
        explain: 'الرمز يُنشَأ مرة واحدة في الذاكرة، والنصّ للبيانات المتغيّرة القادمة من المستخدم.' },
      { q: 'ماذا تُرجع `"RUBY".upcase!`؟', options: ['"RUBY"', '`nil` لأنه لم يحدث تغيير', 'خطأ', '"ruby"'], answer: 1,
        explain: 'لهذا لا تسند نتيجة دالة `!` لمتغيّر أبداً.' },
      { q: 'ما فائدة `user&.profile&.bio`؟', options: ['أسرع', 'يتوقّف ويُرجع nil عند أول قيمة nil بدل رمي NoMethodError', 'يجمّد القيمة', 'يحوّلها لنصّ'], answer: 1,
        explain: 'الاستدعاء الآمن `&.` يختصر سلاسل الاختبارات المتداخلة.' },
      { q: 'لماذا لا نخزّن المال في `Float`؟', options: ['بطيء', 'خطأ التقريب الثنائي يتراكم فينتج فروقاً حقيقية', 'محدود الحجم', 'لا يقبل الكسور'], answer: 1,
        explain: '`0.1 + 0.2 != 0.3`؛ استخدم أعداداً صحيحة بالهللات أو `BigDecimal`.' }
    ]}
  ]
};
